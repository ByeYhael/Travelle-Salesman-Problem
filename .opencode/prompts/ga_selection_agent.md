# ga_selection_agent — Prompt (genérico)

Prepara padres con el método de selección parametrizado en el config — **rank** (probabilidad por posición) o **torneo** (k=3) — sobre la población ordenada mejor→peor. No cruza; re-evalúa tras modificar; confirma.